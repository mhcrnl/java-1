理解度チェック
1.
this.変数名としてインスタンス変数を呼び出す
this();としてコンストラクタを呼び出す
2. 値として引き渡される。（データ型は値のコピー、配列・オブジェクトは参照のコピー）
3. メソッド名と引数のこと

練習問題6.1
1. 変数cはChildクラスの参照であり、ChildクラスはスーパークラスであるParentクラスを参照できないため。

練習問題6.3
1. Hello from C3

練習問題6.4
2. インスタンス化の際に呼び出されたコンストラクタの処理を上書きし、変数の型等が不一致になる可能性があるため。

練習問題6.6
1. できない（abstractは拡張前提だが、finalは拡張できない）
2. 
抽象クラスはサブクラスが持つメソッドや変数を提示するが実際の処理は記述しない。
有形クラスは抽象クラスのメソッドの中身を記述する。

練習問題6.7
2. final修飾子の付いた変数に値を代入することはできないため、コンパイルエラーとなる。

練習問題6.8
1. 
public: 他のクラスからアクセスすることができる。
static: インスタンスメソッドではない。

理解度チェック
1. 一つの実装で複数の処理が行えること。
2. メソッドのオーバーライドを正しく行うこと。
これにより、継承によるメソッドのオーバーライドを行っても正しく処理が行われる。

総合理解度チェック
1. 
オーバーロード：クラス内にシグネチャが異なる同名メソッドが複数存在する場合。
オーバーライド：サブクラスにシグネチャが同一の同名メソッドが存在する場合。
2.
スーパークラスの（1）コンストラクタ（2）メソッド（3）変数を参照する。
